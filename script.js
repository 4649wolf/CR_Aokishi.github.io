(() => {
  const layout = window.PACHINKO_LAYOUT;
  if (!layout) {
    console.error("layout config not found");
    return;
  }

  const COMPLETE_W = layout.completeView.width;
  const COMPLETE_H = layout.completeView.height;
  const SOURCE_W = layout.sourceCanvas.width;
  const SOURCE_H = layout.sourceCanvas.height;
  const MACHINE_SCALE = layout.machinePlacementInComplete.scale;
  const JACKPOT_AWARD = 300;
  const JACKPOT_PRESENT_MS = 5000;
  const RESERVE_MAX = 4;
  const HOT_RESERVE_RATE = {
    smallGoal_01: 0.02,
    smallGoal_02: 0.02,
    goal: 0.045,
    bigGoal: 0.11
  };
  const HOT_REACH_BONUS = 0.28;
  const HOT_JACKPOT_MULTIPLIER = 4.8;
  const REACH_BUTTON_RATE_SCALE = 0.22;
  const HOT_VIDEO_TRIGGER_RATE = 0.88;
  const HOT_VIDEO_FILES = {
    atari: ["./assets/video/atari_01.mp4", "./assets/video/atari_02.mp4"],
    hazure: ["./assets/video/hazure_01.mp4"]
  };

  const GOAL_SETTINGS = {
    smallGoal_01: {
      key: "smallGoal_01",
      label: "小入賞",
      displayOdds: "1/128.0",
      reachRate: 0.52,
      jackpotChance: 1 / 128,
      awardBalls: 2,
      meta: "発展52% / +2玉",
      hotHitFloor: 0.36
    },
    smallGoal_02: {
      key: "smallGoal_02",
      label: "小入賞",
      displayOdds: "1/128.0",
      reachRate: 0.52,
      jackpotChance: 1 / 128,
      awardBalls: 2,
      meta: "発展52% / +2玉",
      hotHitFloor: 0.36
    },
    goal: {
      key: "goal",
      label: "入賞",
      displayOdds: "1/79.8",
      reachRate: 0.72,
      jackpotChance: 1 / 79.8,
      awardBalls: 8,
      meta: "発展72% / +8玉",
      hotHitFloor: 0.52
    },
    bigGoal: {
      key: "bigGoal",
      label: "大入賞",
      displayOdds: "1/26.8",
      reachRate: 0.92,
      jackpotChance: 1 / 26.8,
      awardBalls: 25,
      meta: "発展92% / +25玉",
      hotHitFloor: 0.74
    }
  };

  const PLAYFIELD_BOUNDS = {
    x0: layout.screen.bbox[0],
    y0: layout.screen.bbox[1],
    x1: layout.screen.bbox[2],
    y1: layout.screen.bbox[3],
    rows: [[728.35,772.78],[713.71,786.29],[702.45,797.55],[692.31,807.69],[677.67,823.45],[677.67,823.45],[669.79,830.21],[664.16,835.84],[657.4,842.6],[654.03,845.97],[647.27,852.73],[642.76,857.24],[638.26,861.74],[630.38,869.62],[630.38,869.62],[625.87,874.13],[621.37,878.63],[619.12,882.01],[613.48,886.52],[610.11,889.89],[607.85,893.27],[604.48,895.52],[599.97,900.03],[599.97,900.03],[595.47,905.66],[590.96,909.04],[587.58,912.42],[584.2,915.8],[581.95,919.17],[578.57,921.43],[575.2,924.8],[572.94,927.06],[572.94,927.06],[568.44,932.69],[565.06,936.07],[562.81,937.19],[559.43,940.57],[556.05,943.95],[554.93,946.2],[551.55,949.58],[549.29,950.71],[549.29,950.71],[544.79,955.21],[542.54,957.46],[539.16,960.84],[538.03,963.09],[535.78,965.35],[533.53,966.47],[531
[494.11,1005.89],[496.37,1003.63],[498.62,1001.38],[502.0,998.0],[504.25,995.75],[506.5,993.5],[507.63,993.5],[509.88,991.25],[509.88,991.25],[511.01,988.99],[513.26,986.74],[515.51,984.49],[518.89,981.11],[521.14,978.86],[523.39,976.61],[526.77,974.35],[527.9,972.1],[527.9,972.1],[530.15,969.85],[532.4,967.6],[534.65,965.35],[536.91,963.09],[541.41,958.59],[543.66,957.46],[545.92,955.21],[548.17,951.83],[548.17,951.83],[550.42,949.58],[553.8,946.2],[556.05,943.95],[559.43,941.7],[562.81,937.19],[566.19,933.81],[569.56,930.44],[571.82,928.18],[571.82,928.18],[574.07,925.93],[577.45,922.55],[580.83,919.17],[584.2,915.8],[588.71,911.29],[592.09,907.91],[595.47,904.53],[599.97,901.16],[599.97,901.16],[602.22,897.78],[605.6,894.4],[610.11,889.89],[613.48,887.64],[619.12,880.88],[623.62,876.38],[628.12,873.0],[631.5,868.5],[631.5,868.5],[636.01,863.99],[639.39,860.61],[645.02,854.98],[650.65,850.48],[660.78,840.34],[666.41,833.59],[674.3,826.83],[683.31,817.82],[683.31,817.82],[690.06,809.94],[697.95,802.05],[708.08,791.92],[721.59,778.41]]
  };

  const stageWrap = document.getElementById("stageWrap");
  const stage = document.getElementById("stage");
  const physicsCanvas = document.getElementById("physicsCanvas");
  const ctx = physicsCanvas.getContext("2d");
  const screenEffects = document.getElementById("screenEffects");
  const card = document.getElementById("card");
  const handleWrap = document.getElementById("handleWrap");
  const handlePowerDisplay = document.getElementById("handlePowerDisplay");
  const pushButton = document.getElementById("pushButton");
  const pushButtonImg = document.getElementById("pushButtonImg");
  const resetButton = document.getElementById("resetButton");
  const statusText = document.getElementById("statusText");
  const shotsValue = document.getElementById("shotsValue");
  const goalValue = document.getElementById("goalValue");
  const hitValue = document.getElementById("hitValue");
  const ballValue = document.getElementById("ballValue");
  const holdValue = document.getElementById("holdValue");
  const jackpotAwardValue = document.getElementById("jackpotAwardValue");
  const smallOddsValue = document.getElementById("smallOddsValue");
  const goalOddsValue = document.getElementById("goalOddsValue");
  const bigOddsValue = document.getElementById("bigOddsValue");
  const smallMeta = document.getElementById("smallMeta");
  const goalMeta = document.getElementById("goalMeta");
  const bigMeta = document.getElementById("bigMeta");
  const reelStrips = Array.from(document.querySelectorAll(".reelStrip"));
  const displayVideo = document.getElementById("displayVideo");
  const reserveLane = document.getElementById("reserveLane");
  const reserveSlotsWrap = reserveLane ? reserveLane.querySelector(".reserveSlots") : null;
  const stockCards = Array.from(document.querySelectorAll(".stockCard"));
  const startupModal = document.getElementById("startupModal");
  const startupVolumeRange = document.getElementById("startupVolumeRange");
  const startupVolumeValue = document.getElementById("startupVolumeValue");
  const startupPreviewButton = document.getElementById("startupPreviewButton");
  const startupConfirmButton = document.getElementById("startupConfirmButton");
  const rotateNotice = document.getElementById("rotateNotice");
  const mobileHandleControls = document.getElementById("mobileHandleControls");
  const mobileHandleLeft = document.getElementById("mobileHandleLeft");
  const mobileHandleRight = document.getElementById("mobileHandleRight");

  const GOAL_ENTRIES = [
    { key: "smallGoal_01", label: GOAL_SETTINGS.smallGoal_01.label, center: centerOf(layout.goals.smallGoal_01.entranceBBox) },
    { key: "goal", label: GOAL_SETTINGS.goal.label, center: centerOf(layout.goals.goal.entranceBBox) },
    { key: "smallGoal_02", label: GOAL_SETTINGS.smallGoal_02.label, center: centerOf(layout.goals.smallGoal_02.entranceBBox) },
    { key: "bigGoal", label: GOAL_SETTINGS.bigGoal.label, center: centerOf(layout.goals.bigGoal.entranceBBox) }
  ];
  const OUT_ENTRY = { key: "out", label: "OUT", center: centerOf(layout.out.bbox) };

  const insertedCardRect = sourceRectToStageRect({ x: 64, y: 1659, w: 603, h: 434 });
  const CARD_DROP_RECT = expandRect(insertedCardRect, 180, 120, 160, 130);
  const DISPLAY_ZOOM_MODES = [
    { left: 1124, top: 209, width: 770, height: 434 },
    { left: 515, top: 170, width: 890, height: 502 },
    { left: 420, top: 118, width: 1080, height: 608 }
  ];
  const stockCardHome = new Map(
    stockCards.map((el) => [
      el.id,
      {
        left: parseFloat(el.style.left || getComputedStyle(el).left) || 0,
        top: parseFloat(el.style.top || getComputedStyle(el).top) || 0
      }
    ])
  );

  const state = {
    balls: [],
    shots: 0,
    goals: 0,
    hits: 0,
    power: 0,
    reserveBalls: 0,
    currentBalls: 0,
    masterVolume: 0.8,
    introConfirmed: false,
    introAudioChecked: false,
    popupAudioPlaying: false,
    lastFireAt: 0,
    lastSoundAt: 0,
    lastFrameAt: 0,
    lastResult: "---",
    busySpin: false,
    reach: false,
    pendingSpin: null,
    middleResolveQueued: false,
    jackpotFlashUntil: 0,
    cardInserted: false,
    insertedCardId: null,
    cardShotsLeft: 0,
    ballsPerCard: 250,
    draggingCard: null,
    cardInsertAnimating: false,
    cardInsertTimeoutId: 0,
    reachPressCount: 0,
    hasPlayedKaishiForCard: false,
    kaishiWaiting: false,
    kaishiTimeoutId: 0,
    stageScale: 1,
    stageOffsetX: 0,
    stageOffsetY: 0,
    spinTimeoutIds: [],
    uiTimeoutIds: [],
    reserveQueue: [],
    reserveSeq: 0,
    reserveSpinPending: false,
    hotSequenceStarted: false,
    displayZoomMode: 0,
    spinVisualLockUntil: 0,
    hotResultDelayUntil: 0,
    hotSequenceReleaseAt: 0,
    hotSequenceBlocking: false,
    activeCutsceneToken: 0,
    cutscenePlaying: false,
    mobileHandleDir: 0,
    isMobileUi: false,
    isMobileLandscape: false
  };

  const config = {
    gravity: 1950,
    fireIntervalMs: 125,
    ballRadius: 11.5,
    ballRestitution: 0.86,
    wallRestitution: 0.88,
    linearDamping: 0.0006,
    maxBalls: 42,
    nailRadius: 18,
    collisionSoundThreshold: 190,
    globalSoundCooldownMs: 220,
    perBallSoundCooldownMs: 320,
    lowSpeedThreshold: 18,
    stuckExpireSec: 2.8,
    maxLifeSec: 12
  };

  const soundPool = Array.from({ length: 3 }, () => {
    const a = new Audio("./assets/se/karakara.wav");
    a.volume = 0.05;
    a.preload = "auto";
    a.loop = false;
    return a;
  });

  const cardInsertSound = new Audio("./assets/se/card_insert.mp3");
  cardInsertSound.volume = 0.55;
  cardInsertSound.preload = "auto";
  setBaseVolume(cardInsertSound, cardInsertSound.volume);

  const pushReachFirstSound = new Audio("./assets/se/kyuin_01.wav");
  pushReachFirstSound.volume = 0.82;
  pushReachFirstSound.preload = "auto";

  const pushReachMashSound = new Audio("./assets/se/kyuin_02.wav");
  pushReachMashSound.volume = 0.84;
  pushReachMashSound.preload = "auto";

  const resetSound = new Audio("./assets/se/menu_reset.mp3");
  resetSound.volume = 0.75;
  resetSound.preload = "auto";

  const kaishiSound = new Audio("./assets/se/kaishi.wav");
  kaishiSound.volume = 1.0;
  kaishiSound.preload = "auto";

  const spinBgm = new Audio("./assets/se/start.wav");
  spinBgm.volume = 0.64;
  spinBgm.preload = "auto";
  spinBgm.loop = true;

  const popupConfirmPreviewSound = new Audio("./assets/se/kakuninn.wav");
  popupConfirmPreviewSound.volume = 0.9;
  popupConfirmPreviewSound.preload = "auto";

  const reelStopSound = new Audio("./assets/se/reel_stop.mp3");
  const jackpotSwordSound = new Audio("./assets/se/jackpot_sword.mp3");
  reelStopSound.volume = 0.8;
  reelStopSound.preload = "auto";

  [pushReachFirstSound, pushReachMashSound, resetSound, kaishiSound, spinBgm, popupConfirmPreviewSound, reelStopSound, jackpotSwordSound].forEach((audio) => setBaseVolume(audio, audio.volume));
  soundPool.forEach((audio) => setBaseVolume(audio, audio.volume));

  const nails = layout.nails.centers.map(([x, y]) => ({ x, y, r: config.nailRadius }));
  const walls = buildWalls();
  const reels = [createReel(0), createReel(1), createReel(2)];
  const reserveNodes = new Map();

  function centerOf(box) {
    const [x0, y0, x1, y1] = box;
    return { x: (x0 + x1) * 0.5, y: (y0 + y1) * 0.5 };
  }

  function clamp(value, min, max) {
    return Math.min(max, Math.max(min, value));
  }

  function rand(min, max) {
    return min + Math.random() * (max - min);
  }

  function randInt(min, max) {
    return Math.floor(rand(min, max + 1));
  }

  function pickRandom(list) {
    return Array.isArray(list) && list.length ? list[Math.floor(Math.random() * list.length)] : "";
  }

  function totalHoldBalls() {
    return Math.max(0, Math.round(state.cardInserted ? state.cardShotsLeft : 0));
  }

  function totalAwardBalls() {
    return Math.max(0, Math.round(state.reserveBalls));
  }

  function getBaseVolume(audio) {
    return Number.isFinite(audio?._baseVolume) ? audio._baseVolume : (Number.isFinite(audio?.volume) ? audio.volume : 1);
  }

  function setBaseVolume(audio, volume) {
    if (!audio) return audio;
    audio._baseVolume = clamp(volume, 0, 1.5);
    return audio;
  }

  function effectiveAudioVolume(audio) {
    return clamp(getBaseVolume(audio) * state.masterVolume, 0, 1);
  }

  function reserveAssetPath(type) {
    return type === "hot" ? "./assets/reserve/horyu_02.png" : "./assets/reserve/horyu_01.png";
  }

  function reserveTopForIndex(index) {
    return 6 + index * 86;
  }

  function scheduleUiTask(callback, delayMs) {
    const id = window.setTimeout(() => {
      state.uiTimeoutIds = state.uiTimeoutIds.filter((v) => v !== id);
      callback();
    }, delayMs);
    state.uiTimeoutIds.push(id);
    return id;
  }

  function clearUiTimers() {
    state.uiTimeoutIds.forEach((id) => clearTimeout(id));
    state.uiTimeoutIds = [];
  }

  function clearReserveVisuals() {
    reserveNodes.forEach((node) => node.remove());
    reserveNodes.clear();
  }

  function renderReserveQueue(options = {}) {
    const { enterId = null, upgradeId = null } = options;
    if (!reserveSlotsWrap) return;
    const activeIds = new Set();

    state.reserveQueue.forEach((entry, index) => {
      let node = reserveNodes.get(entry.id);
      const isNew = !node;
      if (!node) {
        node = document.createElement("img");
        node.className = "reserveItem";
        node.draggable = false;
        reserveSlotsWrap.appendChild(node);
        reserveNodes.set(entry.id, node);
      }

      activeIds.add(entry.id);
      node.src = reserveAssetPath(entry.type);
      node.alt = entry.type === "hot" ? "虹保留" : "保留";
      node.style.top = `${reserveTopForIndex(index)}px`;
      node.classList.toggle("isHot", entry.type === "hot");
      node.classList.toggle("isLeaving", !!entry.leaving);

      if (isNew || enterId === entry.id) {
        node.classList.add("isEntering");
        requestAnimationFrame(() => node.classList.remove("isEntering"));
      }

      if (upgradeId === entry.id) {
        node.classList.remove("isUpgrading");
        void node.offsetWidth;
        node.classList.add("isUpgrading");
        scheduleUiTask(() => {
          const liveNode = reserveNodes.get(entry.id);
          if (liveNode) liveNode.classList.remove("isUpgrading");
        }, 580);
      }
    });

    for (const [id, node] of reserveNodes.entries()) {
      if (activeIds.has(id)) continue;
      node.classList.add("isLeaving");
      scheduleUiTask(() => {
        const liveNode = reserveNodes.get(id);
        if (liveNode) liveNode.remove();
        reserveNodes.delete(id);
      }, 220);
    }
  }

  function getReserveType(profile) {
    const rate = HOT_RESERVE_RATE[profile.key] ?? 0.1;
    return Math.random() < rate ? "hot" : "normal";
  }

  function enqueueReserve(profile) {
    if (state.reserveQueue.length >= RESERVE_MAX) {
      setStatus("保留満タン");
      updateHud();
      return;
    }

    const reserveType = getReserveType(profile);
    const entry = {
      id: `reserve_${++state.reserveSeq}`,
      profile,
      type: "normal",
      spinPlan: buildSpinResult(profile, reserveType),
      leaving: false
    };

    state.reserveQueue.push(entry);
    renderReserveQueue({ enterId: entry.id });

    if (reserveType === "hot") {
      scheduleUiTask(() => {
        const liveEntry = state.reserveQueue.find((item) => item.id === entry.id);
        if (!liveEntry) return;
        liveEntry.type = "hot";
        renderReserveQueue({ upgradeId: liveEntry.id });
        setStatus("虹保留に変化！");
      }, 320);
    }

    updateHud();
    scheduleUiTask(() => tryConsumeReserve(), reserveType === "hot" ? 520 : 180);
  }

  function tryConsumeReserve() {
    if (state.busySpin || state.pendingSpin || state.reserveSpinPending || state.hotSequenceBlocking) return;
    if (!state.reserveQueue.length) return;
    if (performance.now() < state.jackpotFlashUntil) return;

    state.reserveSpinPending = true;
    const entry = state.reserveQueue[0];
    entry.leaving = true;
    renderReserveQueue();

    scheduleUiTask(() => {
      const next = state.reserveQueue.shift();
      renderReserveQueue();
      scheduleUiTask(() => {
        state.reserveSpinPending = false;
        state.hotSequenceStarted = false;
        state.spinVisualLockUntil = 0;
        if (!next) return;
        startSpin(next.profile, next.type, next.spinPlan);
      }, 130);
    }, 200);
  }

  function getPlayfieldBoundsAtY(y) {
    const localY = clamp(Math.round(y - PLAYFIELD_BOUNDS.y0), 0, PLAYFIELD_BOUNDS.rows.length - 1);
    const row = PLAYFIELD_BOUNDS.rows[localY];
    return { minX: row[0], maxX: row[1] };
  }

  function constrainBallToPlayfield(ball) {
    const topLimit = PLAYFIELD_BOUNDS.y0 + ball.r;
    const bottomLimit = PLAYFIELD_BOUNDS.y1 - ball.r;

    if (ball.y < topLimit) {
      ball.y = topLimit;
      if (ball.vy < 0) ball.vy = Math.abs(ball.vy) * config.wallRestitution;
    }
    if (ball.y > bottomLimit) {
      ball.y = bottomLimit;
      if (ball.vy > 0) ball.vy = -Math.abs(ball.vy) * config.wallRestitution;
    }

    const { minX, maxX } = getPlayfieldBoundsAtY(ball.y);
    const leftLimit = minX + ball.r;
    const rightLimit = maxX - ball.r;

    if (ball.x < leftLimit) {
      ball.x = leftLimit;
      if (ball.vx < 0) ball.vx = Math.abs(ball.vx) * config.wallRestitution;
    }
    if (ball.x > rightLimit) {
      ball.x = rightLimit;
      if (ball.vx > 0) ball.vx = -Math.abs(ball.vx) * config.wallRestitution;
    }
  }

  function sourceRectToStageRect(rect) {
    return {
      x: layout.machinePlacementInComplete.x + rect.x * MACHINE_SCALE,
      y: layout.machinePlacementInComplete.y + rect.y * MACHINE_SCALE,
      w: rect.w * MACHINE_SCALE,
      h: rect.h * MACHINE_SCALE
    };
  }

  function setDisplayZoom(mode) {
    const nextMode = ((mode % DISPLAY_ZOOM_MODES.length) + DISPLAY_ZOOM_MODES.length) % DISPLAY_ZOOM_MODES.length;
    state.displayZoomMode = nextMode;
    document.body.classList.toggle('displayZoomMode1', nextMode === 1);
    document.body.classList.toggle('displayZoomMode2', nextMode === 2);
    if (!displayViewport) return;
    displayViewport.dataset.zoomMode = String(nextMode);
  }

  function cycleDisplayZoom() {
    setDisplayZoom(state.displayZoomMode + 1);
  }

  function expandRect(rect, left, top, right, bottom) {
    return {
      x: rect.x - left,
      y: rect.y - top,
      w: rect.w + left + right,
      h: rect.h + top + bottom
    };
  }

  function pointInRect(rect, x, y) {
    return x >= rect.x && y >= rect.y && x <= rect.x + rect.w && y <= rect.y + rect.h;
  }

  function resolveStopTarget(reel, symbol, minAdvanceSymbols = 1, extraCycles = 0) {
    const topIndex = (symbol + 6) % 8;
    let target = Math.floor(reel.offset / reel.cycle) * reel.cycle + topIndex * reel.symbolH;
    const minOffset = reel.offset + reel.symbolH * minAdvanceSymbols;
    while (target <= minOffset) target += reel.cycle;
    if (extraCycles > 0) target += reel.cycle * extraCycles;
    return target;
  }

  function createReel(index) {
    const el = reelStrips[index];
    const windowEl = el.parentElement;
    const symbolH = Math.round(windowEl.clientHeight / 3);
    document.getElementById("reelBoard").style.setProperty("--reel-symbol-h", `${symbolH}px`);
    const cycle = 8 * symbolH;
    const repeated = [];
    for (let pass = 0; pass < 4; pass += 1) {
      for (let n = 1; n <= 8; n += 1) {
        repeated.push(`<div class="reelSymbol"><img src="./assets/numbers/${n}.png" alt="${n}" /></div>`);
      }
    }
    el.innerHTML = repeated.join("");
    return { el, symbolH, cycle, offset: cycle, speed: 0, stopTarget: null, spinning: false };
  }

  function renderReel(reel) {
    reel.el.style.transform = `translateY(${-reel.offset}px)`;
    reel.el.classList.toggle("isSpinning", reel.spinning);
  }

  function resetReels() {
    reels.forEach((reel) => {
      reel.offset = reel.cycle;
      reel.speed = 0;
      reel.stopTarget = null;
      reel.spinning = false;
      renderReel(reel);
    });
  }

  function spinAllReels() {
    reels.forEach((reel, index) => {
      reel.spinning = true;
      reel.stopTarget = null;
      reel.speed = index === 1 ? 1680 : 1920;
    });
  }

  function scheduleTask(callback, delayMs) {
    const id = window.setTimeout(() => {
      state.spinTimeoutIds = state.spinTimeoutIds.filter((v) => v !== id);
      callback();
    }, delayMs);
    state.spinTimeoutIds.push(id);
    return id;
  }

  function clearSpinTimers() {
    state.spinTimeoutIds.forEach((id) => clearTimeout(id));
    state.spinTimeoutIds = [];
  }

  function scheduleReelStop(reelIndex, symbol, delayMs, options = {}) {
    const normalized = typeof options === "boolean" ? { slow: options } : (options || {});
    const {
      slow = false,
      speed = slow ? 820 : 1480,
      minAdvanceSymbols = slow ? 0.55 : 0.95,
      extraCycles = 0
    } = normalized;
    scheduleTask(() => {
      const reel = reels[reelIndex];
      reel.el.style.transition = "";
      reel.spinning = true;
      reel.speed = speed;
      reel.stopTarget = resolveStopTarget(reel, symbol, minAdvanceSymbols, extraCycles);
    }, delayMs);
  }

  function snapStepReelToSymbol(reelIndex, symbol, durationMs = 190, playSound = true) {
    const reel = reels[reelIndex];
    const target = resolveStopTarget(reel, symbol, 0.01, 0);
    const visualUnlockAt = performance.now() + durationMs + 50;
    state.spinVisualLockUntil = Math.max(state.spinVisualLockUntil, visualUnlockAt);
    reel.spinning = false;
    reel.speed = 0;
    reel.stopTarget = null;
    reel.el.style.transition = `transform ${durationMs}ms cubic-bezier(0.2, 0.9, 0.22, 1)`;
    reel.offset = target;
    renderReel(reel);
    if (playSound) playReelStopSound();
    scheduleTask(() => {
      reel.el.style.transition = "";
      renderReel(reel);
    }, durationMs + 40);
  }

  function allReelsIdle() {
    if (state.hotSequenceBlocking) return false;
    if (performance.now() < state.spinVisualLockUntil) return false;
    return reels.every((reel) => !reel.spinning && reel.stopTarget == null);
  }

  function updateReels(dtMs) {
    const dt = dtMs / 1000;
    for (const reel of reels) {
      if (!reel.spinning) {
        renderReel(reel);
        continue;
      }
      reel.offset += reel.speed * dt;
      if (reel.stopTarget != null && reel.offset >= reel.stopTarget) {
        reel.offset = reel.stopTarget;
        reel.speed = 0;
        reel.spinning = false;
        reel.stopTarget = null;
        playReelStopSound();
      }
      while (reel.offset >= reel.cycle * 2) {
        reel.offset -= reel.cycle;
        if (reel.stopTarget != null) reel.stopTarget -= reel.cycle;
      }
      renderReel(reel);
    }
    if (state.busySpin && allReelsIdle()) {
      const now = performance.now();
      if (state.hotSequenceReleaseAt && now < state.hotSequenceReleaseAt) {
        return;
      }
      if (state.hotResultDelayUntil && now < state.hotResultDelayUntil) {
        return;
      }
      finishSpin();
    }
  }

  function buildSpinResult(profile, reserveType = "normal") {
    const left = randInt(1, 8);
    const jackpotChance = reserveType === "hot"
      ? Math.min(0.95, Math.max(profile.hotHitFloor ?? 0.4, profile.jackpotChance * HOT_JACKPOT_MULTIPLIER))
      : profile.jackpotChance;
    const reachRate = reserveType === "hot"
      ? 1
      : clamp(profile.reachRate * REACH_BUTTON_RATE_SCALE, 0.12, 0.55);
    const willHit = Math.random() < jackpotChance;
    const willReach = willHit || Math.random() < reachRate;
    const right = reserveType === "hot"
      ? left
      : (willHit || (willReach && Math.random() < 0.84) ? left : randInt(1, 8));
    let middle;
    let teaseStart = 0;
    let hotMissStartBack = 0;
    if (reserveType === "hot") {
      if (willHit) {
        middle = left;
        teaseStart = randInt(3, 5);
      } else {
        middle = wrapSymbol(left + 1);
        hotMissStartBack = 6;
      }
    } else {
      middle = willHit ? left : randInt(1, 8);
      if (!willHit && middle === left && right === left) middle = (middle % 8) + 1;
    }
    const hotVideoPlanned = reserveType === "hot" ? Math.random() < HOT_VIDEO_TRIGGER_RATE : false;
    return { left, middle, right, willHit, willReach, profile, reserveType, teaseStart, hotMissStartBack, hotVideoPlanned };
  }

  function startSpin(profile, reserveType = "normal", preparedPlan = null) {
    if (state.busySpin) return;
    state.busySpin = true;
    state.reach = false;
    state.middleResolveQueued = false;
    state.hotSequenceStarted = false;
    state.spinVisualLockUntil = 0;
    state.hotResultDelayUntil = 0;
    state.hotSequenceReleaseAt = 0;
    state.hotSequenceBlocking = false;
    state.cutscenePlaying = false;
    stopDisplayCutscene();
    clearSpinTimers();
    screenEffects.classList.remove("reach", "jackpot");

    state.pendingSpin = preparedPlan || buildSpinResult(profile, reserveType);
    state.pendingSpin.reserveType = reserveType;
    spinAllReels();
    updateSpinBgmState();
    const isHot = reserveType === "hot";
    if (isHot) setStatus("虹保留変動");
    scheduleReelStop(0, state.pendingSpin.left, isHot ? 760 : 1050, { minAdvanceSymbols: isHot ? 0.75 : 0.95 });
    scheduleReelStop(2, state.pendingSpin.right, isHot ? 1080 : 1520, { minAdvanceSymbols: isHot ? 0.75 : 0.95 });

    if (state.pendingSpin.willReach) {
      scheduleTask(() => activateReach(), isHot ? 1140 : 1540);
    } else {
      scheduleReelStop(1, state.pendingSpin.middle, isHot ? 1580 : 2050, { minAdvanceSymbols: isHot ? 0.8 : 0.95 });
    }
  }

  function activateReach() {
    if (!state.pendingSpin || state.middleResolveQueued) return;
    state.reach = true;
    state.reachPressCount = 0;
    screenEffects.classList.add("reach");
    syncButtonState();
    playOneShot(pushReachMashSound);
    setStatus(state.pendingSpin.reserveType === "hot" ? "虹保留リーチ" : "リーチ");
    reels[1].speed = state.pendingSpin.reserveType === "hot" ? 240 : 820;
    scheduleTask(() => {
      if (state.pendingSpin?.reserveType === "hot") {
        startHotReserveSequence();
      } else {
        resolveReach(false);
      }
    }, state.pendingSpin.reserveType === "hot" ? 720 : 2200);
  }

  function resolveReach() {
    if (!state.pendingSpin || state.middleResolveQueued) return;
    state.middleResolveQueued = true;
    state.reach = false;
    syncButtonState();
    scheduleReelStop(1, state.pendingSpin.middle, 240, { slow: true });
  }

  function wrapSymbol(symbol) {
    return ((symbol - 1 + 8000) % 8) + 1;
  }

  function resumeReelSpin(reelIndex, speed = 520) {
    const reel = reels[reelIndex];
    reel.stopTarget = null;
    reel.speed = speed;
    reel.spinning = true;
  }

  function startHotReserveSequence() {
    if (!state.pendingSpin || state.middleResolveQueued || state.hotSequenceStarted) return;
    state.hotSequenceStarted = true;
    state.middleResolveQueued = true;
    state.reach = false;
    state.hotSequenceBlocking = true;
    syncButtonState();

    const willHit = !!state.pendingSpin.willHit;
    const winningSymbol = state.pendingSpin.left;
    const missFinal = state.pendingSpin.middle;
    const finalSymbol = willHit ? state.pendingSpin.middle : winningSymbol;
    const startBack = willHit
      ? clamp(state.pendingSpin.teaseStart || 4, 3, 5)
      : clamp(state.pendingSpin.hotMissStartBack || 6, 6, 6);

    const stepSymbols = [];
    for (let back = startBack; back >= 1; back -= 1) {
      stepSymbols.push(wrapSymbol(finalSymbol - back));
    }
    stepSymbols.push(finalSymbol);

    const reel = reels[1];
    reel.spinning = false;
    reel.speed = 0;
    reel.stopTarget = null;
    reel.el.style.transition = "";

    const STEP_INTERVAL_MS = 1500;
    const STEP_ANIM_MS = 240;
    const START_DELAY_MS = 90;

    let cursor = START_DELAY_MS;
    stepSymbols.forEach((symbol) => {
      scheduleTask(() => {
        snapStepReelToSymbol(1, symbol, STEP_ANIM_MS, true);
      }, cursor);
      cursor += STEP_INTERVAL_MS;
    });

    const alignRevealDelay = Math.max(0, cursor - STEP_INTERVAL_MS + STEP_ANIM_MS + 40);
    state.spinVisualLockUntil = Math.max(state.spinVisualLockUntil, performance.now() + alignRevealDelay);

    const continueAfterVideo = () => {
      scheduleTask(() => {
        if (!state.pendingSpin) return;
        if (willHit) {
          state.hotSequenceBlocking = false;
          state.hotResultDelayUntil = 0;
          state.hotSequenceReleaseAt = 0;
          state.spinVisualLockUntil = Math.max(state.spinVisualLockUntil, performance.now() + 80);
          return;
        }

        snapStepReelToSymbol(1, missFinal, STEP_ANIM_MS, true);
        state.spinVisualLockUntil = Math.max(state.spinVisualLockUntil, performance.now() + STEP_ANIM_MS + 60);
        scheduleTask(() => {
          state.hotSequenceBlocking = false;
          state.hotResultDelayUntil = 0;
          state.hotSequenceReleaseAt = 0;
        }, STEP_ANIM_MS + 50);
      }, 1500);
    };

    scheduleTask(() => {
      if (!state.pendingSpin) return;
      setStatus(willHit ? "虹保留演出" : "虹保留チャンス");
      if (state.pendingSpin.hotVideoPlanned) {
        playDisplayCutscene(willHit ? "atari" : "hazure", continueAfterVideo);
      } else {
        continueAfterVideo();
      }
    }, alignRevealDelay);
  }

  function finishSpin() {
    state.busySpin = false;
    state.reach = false;
    state.middleResolveQueued = false;
    state.reachPressCount = 0;
    state.spinVisualLockUntil = 0;
    state.hotResultDelayUntil = 0;
    state.hotSequenceReleaseAt = 0;
    state.hotSequenceBlocking = false;
    state.cutscenePlaying = false;
    stopDisplayCutscene();
    syncButtonState();
    screenEffects.classList.remove("reach");

    const result = state.pendingSpin;
    if (!result) return;

    const isJackpot = result.left === result.middle && result.middle === result.right && result.willHit;
    if (isJackpot) {
      state.hits += 1;
      state.reserveBalls += JACKPOT_AWARD;
      state.currentBalls += JACKPOT_AWARD;
      state.lastResult = `${result.left}${result.middle}${result.right} / 大当たり`;
      screenEffects.classList.add("jackpot");
      playOneShot(jackpotSwordSound);
      state.jackpotFlashUntil = performance.now() + JACKPOT_PRESENT_MS;
      state.balls = [];
      state.lastFireAt = performance.now();
      setStatus(`大当たり！ +${JACKPOT_AWARD}玉`);
    } else {
      state.lastResult = `${result.left}${result.middle}${result.right}`;
      setStatus(result.reserveType === "hot" ? "虹保留終了" : "通常停止");
    }
    state.pendingSpin = null;
    updateHud();
    tryConsumeReserve();
    updateSpinBgmState();
  }

  function buildWalls() {
    const built = [];
    const arc = layout.walls.topArc;
    const points = [];
    for (let deg = arc.startDeg; deg <= arc.endDeg; deg += arc.stepDeg) {
      const rad = (deg * Math.PI) / 180;
      points.push({ x: arc.cx + Math.cos(rad) * arc.rx, y: arc.cy + Math.sin(rad) * arc.ry });
    }
    for (let i = 0; i < points.length - 1; i += 1) {
      built.push({ x1: points[i].x, y1: points[i].y, x2: points[i + 1].x, y2: points[i + 1].y, t: 2 });
    }
    for (const [x1, y1, x2, y2] of layout.walls.segments) {
      built.push({ x1, y1, x2, y2, t: 2 });
    }
    return built;
  }

  function testGoalEntry(name, x, y) {
    if (name === "out") {
      const [x0, y0, x1, y1] = layout.out.bbox;
      return x >= x0 && x <= x1 && y >= y0 && y <= y1;
    }
    const [x0, y0, x1, y1] = layout.goals[name].entranceBBox;
    return x >= x0 && x <= x1 && y >= y0 && y <= y1;
  }

  function setStatus(text) {
    statusText.textContent = text;
  }

  function syncButtonState() {
    pushButtonImg.src = state.reach ? "./assets/button/button_2.png" : "./assets/button/button_1.png";
    pushButton.classList.toggle("active", state.reach);
  }

  function updateHud() {
    shotsValue.textContent = String(state.shots);
    goalValue.textContent = String(state.goals);
    hitValue.textContent = String(state.hits);
    ballValue.textContent = String(totalAwardBalls());
    holdValue.textContent = String(totalHoldBalls());
    handlePowerDisplay.textContent = `${Math.round(state.power * 100)}%`;
  }

  function hydrateSettingsPanel() {
    if (displayVideo) {
      displayVideo.removeAttribute("controls");
      displayVideo.muted = false;
      displayVideo.defaultMuted = false;
      displayVideo.loop = false;
      displayVideo.removeAttribute("muted");
      displayVideo.playsInline = true;
      displayVideo.preload = "metadata";
      displayVideo.setAttribute("playsinline", "");
    }
    smallOddsValue.textContent = GOAL_SETTINGS.smallGoal_01.displayOdds;
    goalOddsValue.textContent = GOAL_SETTINGS.goal.displayOdds;
    bigOddsValue.textContent = GOAL_SETTINGS.bigGoal.displayOdds;
    smallMeta.textContent = GOAL_SETTINGS.smallGoal_01.meta;
    goalMeta.textContent = GOAL_SETTINGS.goal.meta;
    bigMeta.textContent = GOAL_SETTINGS.bigGoal.meta;
    jackpotAwardValue.textContent = `+${JACKPOT_AWARD}玉`;
  }

  function stopDisplayCutscene() {
    state.activeCutsceneToken += 1;
    state.cutscenePlaying = false;
    if (!displayViewport || !displayVideo) {
      updateSpinBgmState();
      return;
    }
    displayViewport.classList.remove("isVideoPlaying");
    displayVideo.classList.remove("isActive");
    try {
      displayVideo.pause();
      displayVideo.currentTime = 0;
      displayVideo.onended = null;
      displayVideo.onerror = null;
      displayVideo.onloadedmetadata = null;
      displayVideo.oncanplay = null;
      displayVideo.removeAttribute("src");
      displayVideo.load();
    } catch {}
    updateSpinBgmState();
  }

  function playDisplayCutscene(kind, onDone) {
    if (!displayViewport || !displayVideo) {
      onDone?.();
      return;
    }
    const src = pickRandom(HOT_VIDEO_FILES[kind]);
    if (!src) {
      onDone?.();
      return;
    }

    const token = ++state.activeCutsceneToken;
    state.cutscenePlaying = true;
    stopSpinBgm(false);

    let finished = false;
    let playbackStarted = false;
    let fallbackId = 0;

    const clearVideoHandlers = () => {
      displayVideo.onended = null;
      displayVideo.onerror = null;
      displayVideo.onloadedmetadata = null;
      displayVideo.oncanplay = null;
    };

    const armFallback = (ms) => {
      if (fallbackId) clearTimeout(fallbackId);
      fallbackId = window.setTimeout(() => wrappedFinish(), ms);
    };

    const finish = () => {
      if (finished || token !== state.activeCutsceneToken) return;
      finished = true;
      state.cutscenePlaying = false;
      if (fallbackId) clearTimeout(fallbackId);
      displayViewport.classList.remove("isVideoPlaying");
      displayVideo.classList.remove("isActive");
      clearVideoHandlers();
      try {
        displayVideo.pause();
        displayVideo.currentTime = 0;
      } catch {}
      onDone?.();
      updateSpinBgmState();
    };

    const wrappedFinish = () => finish();

    const startPlayback = () => {
      if (playbackStarted || token !== state.activeCutsceneToken) return;
      playbackStarted = true;
      displayVideo.volume = effectiveAudioVolume({ _baseVolume: 1 });
      const maybePromise = displayVideo.play();
      if (maybePromise && typeof maybePromise.catch === "function") {
        maybePromise.catch(() => {
          if (!Number.isFinite(displayVideo.duration) || displayVideo.duration <= 0) {
            armFallback(12000);
          }
        });
      }
    };

    displayViewport.classList.add("isVideoPlaying");
    displayVideo.classList.add("isActive");
    displayVideo.pause();
    displayVideo.currentTime = 0;
    displayVideo.loop = false;
    displayVideo.muted = false;
    displayVideo.defaultMuted = false;
    displayVideo.removeAttribute("muted");
    displayVideo.src = src;
    displayVideo.volume = effectiveAudioVolume({ _baseVolume: 1 });
    displayVideo.onended = wrappedFinish;
    displayVideo.onerror = wrappedFinish;
    displayVideo.onloadedmetadata = () => {
      const durationMs = Number.isFinite(displayVideo.duration) && displayVideo.duration > 0
        ? Math.ceil(displayVideo.duration * 1000) + 1200
        : 12000;
      armFallback(durationMs);
    };
    displayVideo.oncanplay = startPlayback;

    try { displayVideo.load(); } catch {}
    armFallback(12000);
    startPlayback();
  }

  function playKarakara(ball) {
    const now = performance.now();
    if (ball && now - (ball.lastSoundAt || 0) < config.perBallSoundCooldownMs) return;
    if (now - state.lastSoundAt < config.globalSoundCooldownMs) return;
    state.lastSoundAt = now;
    if (ball) ball.lastSoundAt = now;
    const audio = soundPool.find((a) => a.paused || a.ended);
    if (!audio) return;
    try {
      audio.currentTime = 0;
      audio.volume = effectiveAudioVolume(audio);
      void audio.play();
    } catch {
      // local file autoplay can be a tiny gremlin
    }
  }

  function playOneShot(audio) {
    try {
      const cloned = audio.cloneNode(true);
      cloned.volume = effectiveAudioVolume(audio);
      cloned.currentTime = 0;
      const maybePromise = cloned.play();
      if (maybePromise && typeof maybePromise.catch === "function") {
        maybePromise.catch(() => {});
      }
    } catch {
      try {
        audio.currentTime = 0;
        audio.volume = effectiveAudioVolume(audio);
        const maybePromise = audio.play();
        if (maybePromise && typeof maybePromise.catch === "function") {
          maybePromise.catch(() => {});
        }
      } catch {
        // local autoplay policy can be a tiny gremlin
      }
    }
  }

  function updateMasterVolume(value) {
    const normalized = clamp(Number(value) / 100, 0, 1);
    state.masterVolume = normalized;
    if (startupVolumeValue) startupVolumeValue.textContent = `${Math.round(normalized * 100)}%`;
  }

  function setStartupConfirmReady(isReady) {
    if (!startupConfirmButton) return;
    startupConfirmButton.disabled = !isReady;
    startupConfirmButton.setAttribute("aria-disabled", String(!isReady));
    startupConfirmButton.classList.toggle("isReady", !!isReady);
  }

  function playPopupVolumePreview() {
    if (!popupConfirmPreviewSound || state.popupAudioPlaying) return;
    state.popupAudioPlaying = true;
    state.introAudioChecked = false;
    setStartupConfirmReady(false);
    startupPreviewButton?.setAttribute("disabled", "disabled");
    popupConfirmPreviewSound.currentTime = 0;
    popupConfirmPreviewSound.volume = effectiveAudioVolume(popupConfirmPreviewSound);

    const onDone = () => {
      popupConfirmPreviewSound.removeEventListener("ended", onDone);
      popupConfirmPreviewSound.removeEventListener("error", onDone);
      state.popupAudioPlaying = false;
      state.introAudioChecked = true;
      startupPreviewButton?.removeAttribute("disabled");
      setStartupConfirmReady(true);
    };

    popupConfirmPreviewSound.addEventListener("ended", onDone, { once: true });
    popupConfirmPreviewSound.addEventListener("error", onDone, { once: true });
    const maybePromise = popupConfirmPreviewSound.play();
    if (maybePromise && typeof maybePromise.catch === "function") {
      maybePromise.catch(() => onDone());
    }
  }

  function closeStartupModal() {
    if (!startupModal || !state.introAudioChecked) return;
    state.introConfirmed = true;
    playOneShot(pushReachMashSound);
    startupModal.classList.add("isHidden");
    scheduleUiTask(() => {
      startupModal.style.display = "none";
    }, 260);
  }

  function playCardInsert() {
    playOneShot(cardInsertSound);
  }

  function playReachPush(firstPress) {
    playOneShot(firstPress ? pushReachFirstSound : pushReachMashSound);
  }

  function playResetSound() {
    playOneShot(resetSound);
  }

  function playReelStopSound() {
    playOneShot(reelStopSound);
  }

  function playKaishiSound(onEnded) {
    let finished = false;
    const finish = () => {
      if (finished) return;
      finished = true;
      onEnded?.();
    };

    try {
      const cloned = kaishiSound.cloneNode(true);
      cloned.volume = effectiveAudioVolume(kaishiSound);
      cloned.currentTime = 0;
      cloned.addEventListener("ended", finish, { once: true });
      cloned.addEventListener("error", finish, { once: true });
      const fallbackMs = Number.isFinite(cloned.duration) && cloned.duration > 0 ? cloned.duration * 1000 + 40 : 1100;
      window.setTimeout(finish, fallbackMs);
      const maybePromise = cloned.play();
      if (maybePromise && typeof maybePromise.catch === "function") {
        maybePromise.catch(() => finish());
      }
      return;
    } catch {
      // fall through to base audio below
    }

    try {
      kaishiSound.currentTime = 0;
      kaishiSound.volume = effectiveAudioVolume(kaishiSound);
      kaishiSound.onended = finish;
      const fallbackMs = Number.isFinite(kaishiSound.duration) && kaishiSound.duration > 0 ? kaishiSound.duration * 1000 + 40 : 1100;
      window.setTimeout(finish, fallbackMs);
      const maybePromise = kaishiSound.play();
      if (maybePromise && typeof maybePromise.catch === "function") {
        maybePromise.catch(() => finish());
      }
    } catch {
      finish();
    }
  }

  function shouldKeepSpinBgm() {
    if (state.cutscenePlaying) return false;
    return !!(state.busySpin || state.pendingSpin || state.reserveSpinPending || state.reserveQueue.length > 0 || state.reach || state.middleResolveQueued || state.hotSequenceBlocking || state.hotSequenceStarted);
  }

  function playSpinBgm() {
    if (!spinBgm) return;
    try {
      spinBgm.volume = effectiveAudioVolume(spinBgm);
      if (spinBgm.paused) {
        spinBgm.currentTime = 0;
        const maybePromise = spinBgm.play();
        if (maybePromise && typeof maybePromise.catch === "function") maybePromise.catch(() => {});
      }
    } catch {}
  }

  function stopSpinBgm(resetTime = true) {
    if (!spinBgm) return;
    try {
      spinBgm.pause();
      if (resetTime) spinBgm.currentTime = 0;
    } catch {}
  }

  function updateSpinBgmState() {
    if (shouldKeepSpinBgm()) playSpinBgm();
    else stopSpinBgm(true);
  }

  function consumeCurrentCard() {
    state.cardShotsLeft = 0;
    state.kaishiWaiting = false;
    if (state.kaishiTimeoutId) {
      clearTimeout(state.kaishiTimeoutId);
      state.kaishiTimeoutId = 0;
    }
    if (!state.insertedCardId) {
      state.cardInserted = false;
      hideInsertedCard();
      updateHud();
      return;
    }
    const currentCardEl = document.getElementById(state.insertedCardId);
    if (currentCardEl) {
      currentCardEl.classList.add("used");
      currentCardEl.style.opacity = "0";
      currentCardEl.style.pointerEvents = "none";
    }
    state.insertedCardId = null;
    state.cardInserted = false;
    state.hasPlayedKaishiForCard = false;
    hideInsertedCard();
    setStatus("カード切れです。次のカードを挿入してください");
    updateHud();
  }

  function createBall() {
    if (!state.cardInserted || state.cardShotsLeft <= 0 || state.balls.length >= config.maxBalls) return;
    const spawn = layout.spawn.spawnPoint;
    const ball = {
      id: `${Date.now()}_${Math.random()}`,
      x: spawn.x + rand(-6, 6),
      y: spawn.y - 8,
      vx: rand(-120, 120) * (0.42 + state.power * 0.78),
      vy: rand(90, 160) + state.power * 120,
      r: config.ballRadius,
      captured: null,
      life: 0,
      fade: 1,
      fading: false,
      stuckFor: 0,
      lastSoundAt: 0
    };
    state.cardShotsLeft -= 1;
    state.currentBalls = Math.max(0, state.currentBalls - 1);
    state.shots += 1;
    state.balls.push(ball);
    if (state.cardShotsLeft <= 0) consumeCurrentCard();
    updateHud();
  }

  function maybeFire() {
    const now = performance.now();
    if (!state.introConfirmed) return;
    if (now < state.jackpotFlashUntil) return;
    if (state.power <= 0 || !state.cardInserted) return;
    if (state.kaishiWaiting) return;
    if (!state.hasPlayedKaishiForCard) {
      state.kaishiWaiting = true;
      state.lastFireAt = now;
      const activeCardId = state.insertedCardId;
      playKaishiSound(() => {
        if (!state.cardInserted || state.insertedCardId !== activeCardId) return;
        state.hasPlayedKaishiForCard = true;
        state.kaishiWaiting = false;
        state.lastFireAt = 0;
      });
      return;
    }
    if (now - state.lastFireAt < config.fireIntervalMs) return;
    state.lastFireAt = now;
    createBall();
  }

  function updateHandleVisual() {
    const rotate = state.power * 180;
    const glow = state.power > 0 ? "drop-shadow(0 0 16px rgba(255,120,120,0.35))" : "none";
    handleWrap.style.transform = `rotate(${rotate.toFixed(2)}deg)`;
    handleWrap.style.filter = glow;
  }

  function onScored(goalKey) {
    const profile = GOAL_SETTINGS[goalKey];
    if (!profile) return;
    state.goals += 1;
    state.reserveBalls += profile.awardBalls;
    state.currentBalls += profile.awardBalls;
    state.lastResult = profile.label;
    enqueueReserve(profile);
  }

  function updateBall(ball, dt) {
    ball.life += dt;

    if (ball.captured) {
      ball.x += (ball.captured.targetX - ball.x) * 0.24;
      ball.y += (ball.captured.targetY - ball.y) * 0.24;
      ball.vx *= 0.84;
      ball.vy *= 0.84;
      ball.captured.frames += 1;
      if (ball.captured.frames > 12) {
        ball.dead = true;
        if (ball.captured.type === "out") {
          state.lastResult = "OUT";
          
          updateHud();
        } else {
          onScored(ball.captured.goalKey);
        }
      }
      return;
    }

    ball.vy += config.gravity * dt;
    ball.vx *= 1 - config.linearDamping;
    ball.vy *= 1 - config.linearDamping;
    ball.x += ball.vx * dt;
    ball.y += ball.vy * dt;

    let collisionPower = 0;
    for (const wall of walls) collisionPower = Math.max(collisionPower, collideBallWithSegment(ball, wall));
    for (const nail of nails) collisionPower = Math.max(collisionPower, collideBallWithCircle(ball, nail.x, nail.y, nail.r, config.ballRestitution));
    constrainBallToPlayfield(ball);
    if (collisionPower > config.collisionSoundThreshold) playKarakara(ball);

    const speed = Math.hypot(ball.vx, ball.vy);
    const likelyStuck = speed < config.lowSpeedThreshold && ball.y > 1180;
    ball.stuckFor = likelyStuck ? ball.stuckFor + dt : Math.max(0, ball.stuckFor - dt * 0.35);
    if (ball.stuckFor > config.stuckExpireSec || ball.life > config.maxLifeSec) {
      ball.fading = true;
    }
    if (ball.fading) {
      ball.fade = clamp(ball.fade - dt * 1.6, 0, 1);
      if (ball.fade <= 0.03) {
        ball.dead = true;
        return;
      }
    }

    for (const goal of GOAL_ENTRIES) {
      if (testGoalEntry(goal.key, ball.x, ball.y)) {
        ball.captured = {
          type: "goal",
          label: goal.label,
          goalKey: goal.key,
          targetX: goal.center.x,
          targetY: goal.center.y + 22,
          frames: 0
        };
        return;
      }
    }

    if (testGoalEntry("out", ball.x, ball.y)) {
      ball.captured = {
        type: "out",
        label: "OUT",
        targetX: OUT_ENTRY.center.x,
        targetY: OUT_ENTRY.center.y + 12,
        frames: 0
      };
      return;
    }

    if (ball.y > SOURCE_H + 120 || ball.x < -120 || ball.x > SOURCE_W + 120) {
      ball.dead = true;
    }
  }

  function collideBallWithCircle(ball, cx, cy, radius, restitution) {
    const dx = ball.x - cx;
    const dy = ball.y - cy;
    const distSq = dx * dx + dy * dy;
    const minDist = ball.r + radius;
    if (distSq >= minDist * minDist) return 0;
    const dist = Math.sqrt(distSq) || 0.0001;
    const nx = dx / dist;
    const ny = dy / dist;
    const overlap = minDist - dist;
    ball.x += nx * overlap;
    ball.y += ny * overlap;
    const vn = ball.vx * nx + ball.vy * ny;
    if (vn < 0) {
      const bounce = -(1 + restitution) * vn;
      ball.vx += bounce * nx;
      ball.vy += bounce * ny;
    }
    return Math.abs(vn);
  }

  function collideBallWithSegment(ball, seg) {
    const dx = seg.x2 - seg.x1;
    const dy = seg.y2 - seg.y1;
    const lengthSq = dx * dx + dy * dy || 1;
    let t = ((ball.x - seg.x1) * dx + (ball.y - seg.y1) * dy) / lengthSq;
    t = clamp(t, 0, 1);
    const px = seg.x1 + dx * t;
    const py = seg.y1 + dy * t;
    let nx = ball.x - px;
    let ny = ball.y - py;
    let dist = Math.hypot(nx, ny);
    const minDist = ball.r + seg.t;
    if (dist >= minDist) return 0;
    if (dist < 0.0001) {
      nx = -dy;
      ny = dx;
      dist = Math.hypot(nx, ny) || 1;
    }
    nx /= dist;
    ny /= dist;
    const overlap = minDist - dist;
    ball.x += nx * overlap;
    ball.y += ny * overlap;
    const vn = ball.vx * nx + ball.vy * ny;
    if (vn < 0) {
      const bounce = -(1 + config.wallRestitution) * vn;
      ball.vx += bounce * nx;
      ball.vy += bounce * ny;
    }
    return Math.abs(vn);
  }

  function solveBallBallCollisions() {
    const balls = state.balls;
    for (let i = 0; i < balls.length; i += 1) {
      const a = balls[i];
      if (a.captured) continue;
      for (let j = i + 1; j < balls.length; j += 1) {
        const b = balls[j];
        if (b.captured) continue;
        const dx = b.x - a.x;
        const dy = b.y - a.y;
        const distSq = dx * dx + dy * dy;
        const minDist = a.r + b.r;
        if (distSq >= minDist * minDist || distSq < 0.0001) continue;
        const dist = Math.sqrt(distSq);
        const nx = dx / dist;
        const ny = dy / dist;
        const overlap = minDist - dist;
        a.x -= nx * overlap * 0.5;
        a.y -= ny * overlap * 0.5;
        b.x += nx * overlap * 0.5;
        b.y += ny * overlap * 0.5;
        const rvx = b.vx - a.vx;
        const rvy = b.vy - a.vy;
        const vn = rvx * nx + rvy * ny;
        if (vn > 0) continue;
        const impulse = -(1 + 0.86) * vn * 0.5;
        a.vx -= impulse * nx;
        a.vy -= impulse * ny;
        b.vx += impulse * nx;
        b.vy += impulse * ny;
      }
    }
  }

  function drawBall(ball) {
    const alpha = ball.fade ?? 1;
    const g = ctx.createRadialGradient(ball.x - ball.r * 0.45, ball.y - ball.r * 0.55, 1, ball.x, ball.y, ball.r * 1.15);
    g.addColorStop(0, `rgba(255,255,255,${0.98 * alpha})`);
    g.addColorStop(0.28, `rgba(247,247,247,${0.94 * alpha})`);
    g.addColorStop(0.64, `rgba(184,188,196,${0.96 * alpha})`);
    g.addColorStop(1, `rgba(110,115,124,${0.96 * alpha})`);

    ctx.beginPath();
    ctx.arc(ball.x, ball.y, ball.r, 0, Math.PI * 2);
    ctx.fillStyle = g;
    ctx.fill();

    ctx.lineWidth = 1.5;
    ctx.strokeStyle = `rgba(60,60,70,${0.72 * alpha})`;
    ctx.stroke();

    ctx.beginPath();
    ctx.arc(ball.x - ball.r * 0.22, ball.y - ball.r * 0.28, ball.r * 0.32, 0, Math.PI * 2);
    ctx.fillStyle = `rgba(255,255,255,${0.55 * alpha})`;
    ctx.fill();
  }

  function drawScene() {
    ctx.clearRect(0, 0, SOURCE_W, SOURCE_H);
    for (const ball of state.balls) drawBall(ball);
  }

  function detectMobileUiMode() {
    const coarsePointer = window.matchMedia("(pointer: coarse)").matches || navigator.maxTouchPoints > 0;
    const narrowSide = Math.min(window.innerWidth, window.innerHeight);
    const mobileLike = coarsePointer && narrowSide <= 1024;
    const landscape = mobileLike && window.innerWidth >= window.innerHeight;
    return { mobileLike, landscape };
  }

  function setMobileHandleDirection(dir) {
    state.mobileHandleDir = dir;
    if (mobileHandleLeft) mobileHandleLeft.classList.toggle("isPressed", dir < 0);
    if (mobileHandleRight) mobileHandleRight.classList.toggle("isPressed", dir > 0);
  }

  function updateMobileMode() {
    const mode = detectMobileUiMode();
    state.isMobileUi = mode.mobileLike;
    state.isMobileLandscape = mode.landscape;
    document.body.classList.toggle("isMobileUi", mode.mobileLike);
    document.body.classList.toggle("isMobilePortrait", mode.mobileLike && !mode.landscape);
    document.body.classList.toggle("isMobileLandscape", mode.landscape);
    if (rotateNotice) {
      rotateNotice.setAttribute("aria-hidden", mode.mobileLike && !mode.landscape ? "false" : "true");
    }
    if (!mode.landscape) {
      setMobileHandleDirection(0);
    }
  }

  function fitStage() {
    const pad = state.isMobileLandscape ? 8 : 18;
    const safeW = Math.max(320, stageWrap.clientWidth - pad * 2);
    const safeH = Math.max(320, stageWrap.clientHeight - pad * 2);
    state.stageScale = Math.min(1, safeW / COMPLETE_W, safeH / COMPLETE_H);
    state.stageOffsetX = Math.max(pad, (stageWrap.clientWidth - COMPLETE_W * state.stageScale) * 0.5);
    state.stageOffsetY = Math.max(pad, (stageWrap.clientHeight - COMPLETE_H * state.stageScale) * 0.5);
    stage.style.transform = `translate(${state.stageOffsetX}px, ${state.stageOffsetY}px) scale(${state.stageScale})`;
  }

  function clientToStage(clientX, clientY) {
    const rect = stage.getBoundingClientRect();
    return {
      x: (clientX - rect.left) / state.stageScale,
      y: (clientY - rect.top) / state.stageScale
    };
  }

  function setPower(value) {
    state.power = clamp(value, 0, 1);
    updateHandleVisual();
    updateHud();
  }

  function nudgePower(delta) {
    setPower(state.power + delta);
  }

  function showInsertedCard(startStageLeft = null, startStageTop = null) {
    const finalStageLeft = insertedCardRect.x;
    const finalStageTop = insertedCardRect.y;
    const startX = startStageLeft == null ? 0 : (startStageLeft - finalStageLeft) / MACHINE_SCALE;
    const startY = startStageTop == null ? 0 : (startStageTop - finalStageTop) / MACHINE_SCALE;

    card.style.setProperty("--insert-start-x", `${startX}px`);
    card.style.setProperty("--insert-start-y", `${startY}px`);
    card.classList.remove("inserted", "animatingInsert", "run", "visible");
    void card.offsetWidth;
    card.classList.add("visible", "animatingInsert");
    requestAnimationFrame(() => card.classList.add("run"));
  }

  function hideInsertedCard() {
    card.classList.remove("inserted", "animatingInsert", "run", "visible");
    card.style.removeProperty("--insert-start-x");
    card.style.removeProperty("--insert-start-y");
  }

  function restoreStockCard(cardEl) {
    const home = stockCardHome.get(cardEl.id);
    cardEl.classList.remove("used", "dragging", "inserting");
    cardEl.style.left = `${home.left}px`;
    cardEl.style.top = `${home.top}px`;
    cardEl.style.zIndex = "";
    cardEl.style.opacity = "";
    cardEl.style.pointerEvents = "";
  }

  function clearInsertedCard() {
    state.insertedCardId = null;
    state.cardInserted = false;
    state.cardShotsLeft = 0;
    state.hasPlayedKaishiForCard = false;
    state.cardInsertAnimating = false;
    state.reachPressCount = 0;
    state.hasPlayedKaishiForCard = false;
    state.kaishiWaiting = false;
    state.kaishiWaiting = false;
    if (state.cardInsertTimeoutId) {
      clearTimeout(state.cardInsertTimeoutId);
      state.cardInsertTimeoutId = 0;
    }
    if (state.kaishiTimeoutId) {
      clearTimeout(state.kaishiTimeoutId);
      state.kaishiTimeoutId = 0;
    }
    clearUiTimers();
    hideInsertedCard();
  }

  function finalizeInsertedCard(cardEl) {
    state.cardInsertAnimating = false;
    state.reachPressCount = 0;
    state.hasPlayedKaishiForCard = false;
    state.kaishiWaiting = false;
    state.kaishiWaiting = false;
    state.cardInsertTimeoutId = 0;
    if (!state.cardInserted) {
      state.cardInserted = true;
      state.insertedCardId = cardEl.id;
      state.cardShotsLeft = state.ballsPerCard;
    }
    card.classList.remove("animatingInsert", "run");
    card.classList.add("inserted", "visible");
    setStatus("カード装填完了");
    updateHud();
  }

  function insertCard(cardEl) {
    if (state.cardInserted || state.cardInsertAnimating) {
      restoreStockCard(cardEl);
      setStatus("カードを使い切ってから挿入してください");
      return;
    }

    const stageLeft = parseFloat(cardEl.style.left || getComputedStyle(cardEl).left) || 0;
    const stageTop = parseFloat(cardEl.style.top || getComputedStyle(cardEl).top) || 0;

    state.cardInsertAnimating = true;
    state.cardInserted = true;
    state.insertedCardId = cardEl.id;
    state.cardShotsLeft = state.ballsPerCard;
    state.currentBalls += state.ballsPerCard;
    state.hasPlayedKaishiForCard = false;
    state.kaishiWaiting = false;

    cardEl.classList.add("used", "inserting");
    cardEl.style.opacity = "0";
    cardEl.style.pointerEvents = "none";

    showInsertedCard(stageLeft, stageTop);
    playCardInsert();
    setStatus("カード装填完了");
    updateHud();

    if (state.cardInsertTimeoutId) clearTimeout(state.cardInsertTimeoutId);
    state.cardInsertTimeoutId = window.setTimeout(() => finalizeInsertedCard(cardEl), 640);
  }

  function startCardDrag(cardEl, event) {
    if (cardEl.classList.contains("used") || state.cardInserted || state.cardInsertAnimating) {
      if (state.cardInserted || state.cardInsertAnimating) setStatus("カードを使い切ってから挿入してください");
      return;
    }
    const pos = clientToStage(event.clientX, event.clientY);
    const currentLeft = parseFloat(cardEl.style.left || getComputedStyle(cardEl).left) || 0;
    const currentTop = parseFloat(cardEl.style.top || getComputedStyle(cardEl).top) || 0;
    state.draggingCard = {
      el: cardEl,
      pointerId: event.pointerId,
      offsetX: pos.x - currentLeft,
      offsetY: pos.y - currentTop
    };
    cardEl.classList.add("dragging");
    cardEl.style.zIndex = "20";
    cardEl.setPointerCapture(event.pointerId);
    event.preventDefault();
  }

  function moveDraggingCard(event) {
    if (!state.draggingCard || state.draggingCard.pointerId !== event.pointerId) return;
    const pos = clientToStage(event.clientX, event.clientY);
    const left = pos.x - state.draggingCard.offsetX;
    const top = pos.y - state.draggingCard.offsetY;
    state.draggingCard.el.style.left = `${left}px`;
    state.draggingCard.el.style.top = `${top}px`;
  }

  function endCardDrag(event) {
    if (!state.draggingCard || state.draggingCard.pointerId !== event.pointerId) return;
    const drag = state.draggingCard;
    const el = drag.el;
    const left = parseFloat(el.style.left || "0");
    const top = parseFloat(el.style.top || "0");
    const centerX = left + el.offsetWidth * 0.5;
    const centerY = top + el.offsetHeight * 0.5;

    el.classList.remove("dragging");
    el.style.zIndex = "";
    if (pointInRect(CARD_DROP_RECT, centerX, centerY)) {
      insertCard(el);
    } else {
      restoreStockCard(el);
      setStatus("カードをドラッグして挿入してください");
    }
    try { el.releasePointerCapture(event.pointerId); } catch {}
    state.draggingCard = null;
  }

  function animateResetButton() {
    resetButton.classList.add("isPressed");
    window.setTimeout(() => resetButton.classList.remove("isPressed"), 120);
  }

  function resetGame() {
    clearSpinTimers();
    state.balls = [];
    state.shots = 0;
    state.goals = 0;
    state.hits = 0;
    state.power = 0;
    state.reserveBalls = 0;
    state.currentBalls = 0;
    state.lastFireAt = 0;
    state.lastSoundAt = 0;
    state.lastResult = "---";
    state.busySpin = false;
    state.reach = false;
    state.pendingSpin = null;
    state.middleResolveQueued = false;
    state.jackpotFlashUntil = 0;
    state.cardInserted = false;
    state.insertedCardId = null;
    state.cardShotsLeft = 0;
    state.draggingCard = null;
    state.cardInsertAnimating = false;
    state.reachPressCount = 0;
    state.hasPlayedKaishiForCard = false;
    state.kaishiWaiting = false;
    state.reserveQueue = [];
    state.reserveSeq = 0;
    state.reserveSpinPending = false;
    state.hotSequenceStarted = false;
    state.spinVisualLockUntil = 0;
    state.hotResultDelayUntil = 0;
    state.hotSequenceReleaseAt = 0;
    state.hotSequenceBlocking = false;
    state.cutscenePlaying = false;
    stopDisplayCutscene();
    if (state.cardInsertTimeoutId) {
      clearTimeout(state.cardInsertTimeoutId);
      state.cardInsertTimeoutId = 0;
    }
    if (state.kaishiTimeoutId) {
      clearTimeout(state.kaishiTimeoutId);
      state.kaishiTimeoutId = 0;
    }
    clearUiTimers();
    stopDisplayCutscene();
    stopSpinBgm(true);
    screenEffects.classList.remove("reach", "jackpot");
    pushButton.classList.remove("isPressed");
    syncButtonState();
    hideInsertedCard();
    clearReserveVisuals();
    stockCards.forEach((cardEl) => restoreStockCard(cardEl));
    resetReels();
    setStatus("カードをドラッグして挿入してください");
    updateHandleVisual();
    updateHud();
    drawScene();
  }

  function mashReachButton() {
    if (performance.now() < state.jackpotFlashUntil) return;
    if (!state.reach || state.middleResolveQueued) return;
    if (state.reachPressCount > 0) return;
    state.reachPressCount = 1;
    playOneShot(pushReachFirstSound);
    pushButton.classList.remove("isPressed");
    requestAnimationFrame(() => pushButton.classList.add("isPressed"));
    window.setTimeout(() => pushButton.classList.remove("isPressed"), 120);

    if (!state.reach || state.middleResolveQueued) return;

    if (state.pendingSpin?.reserveType === "hot") {
      setStatus("虹保留演出中");
      return;
    }

    resolveReach();
  }

  function bindEvents() {
    stockCards.forEach((cardEl) => {
      cardEl.addEventListener("pointerdown", (event) => startCardDrag(cardEl, event));
      cardEl.addEventListener("pointermove", moveDraggingCard);
      cardEl.addEventListener("pointerup", endCardDrag);
      cardEl.addEventListener("pointercancel", endCardDrag);
    });

    pushButton.addEventListener("click", () => {
      if (state.reach || state.middleResolveQueued) mashReachButton();
    });

    resetButton.addEventListener("pointerdown", () => resetButton.classList.add("isPressed"));
    resetButton.addEventListener("pointerup", () => resetButton.classList.remove("isPressed"));
    resetButton.addEventListener("pointerleave", () => resetButton.classList.remove("isPressed"));
    resetButton.addEventListener("click", () => {
      animateResetButton();
      playResetSound();
      resetGame();
    });

    startupVolumeRange?.addEventListener("input", (event) => {
      updateMasterVolume(event.currentTarget.value);
    });

    startupPreviewButton?.addEventListener("click", () => {
      playPopupVolumePreview();
    });

    startupConfirmButton?.addEventListener("click", () => {
      if (startupConfirmButton.disabled) return;
      closeStartupModal();
    });

    displayViewport?.addEventListener("click", (event) => {
      if (!state.introConfirmed) return;
      event.preventDefault();
      cycleDisplayZoom();
    });

    const stopMobileHandle = () => setMobileHandleDirection(0);
    mobileHandleLeft?.addEventListener("pointerdown", (event) => {
      event.preventDefault();
      setMobileHandleDirection(-1);
    });
    mobileHandleRight?.addEventListener("pointerdown", (event) => {
      event.preventDefault();
      setMobileHandleDirection(1);
    });
    [mobileHandleLeft, mobileHandleRight].forEach((btn) => {
      btn?.addEventListener("pointerup", stopMobileHandle);
      btn?.addEventListener("pointercancel", stopMobileHandle);
      btn?.addEventListener("pointerleave", stopMobileHandle);
      btn?.addEventListener("lostpointercapture", stopMobileHandle);
      btn?.addEventListener("contextmenu", (event) => event.preventDefault());
    });

    const handleViewportChange = () => {
      updateMobileMode();
      fitStage();
    };
    window.addEventListener("resize", handleViewportChange);
    window.addEventListener("orientationchange", handleViewportChange);
    window.addEventListener("keydown", (event) => {
      if (event.code === "ArrowLeft") {
        nudgePower(-0.02);
        event.preventDefault();
      } else if (event.code === "ArrowRight") {
        nudgePower(0.02);
        event.preventDefault();
      } else if (event.code === "Space") {
        if (state.reach || state.middleResolveQueued) mashReachButton();
        event.preventDefault();
      } else if (event.key.toLowerCase() === "r") {
        playResetSound();
        resetGame();
      }
    });
  }

  function loop(now) {
    const dtMs = Math.min(32, now - state.lastFrameAt || 16.67);
    state.lastFrameAt = now;
    const dt = dtMs / 1000;

    if (state.jackpotFlashUntil && now > state.jackpotFlashUntil) {
      screenEffects.classList.remove("jackpot");
      state.jackpotFlashUntil = 0;
    }

    if (state.mobileHandleDir !== 0 && state.introConfirmed && state.isMobileLandscape) {
      nudgePower(state.mobileHandleDir * dtMs * 0.00075);
    }

    maybeFire();
    tryConsumeReserve();
    updateSpinBgmState();

    for (const ball of state.balls) updateBall(ball, dt);
    solveBallBallCollisions();
    state.balls = state.balls.filter((ball) => !ball.dead);

    updateReels(dtMs);
    drawScene();
    updateHud();

    requestAnimationFrame(loop);
  }

  function init() {
    hydrateSettingsPanel();
    updateMobileMode();
    fitStage();
    bindEvents();
    clearReserveVisuals();
    stopDisplayCutscene();
    stopSpinBgm(true);
    resetReels();
    setDisplayZoom(0);
    updateMasterVolume(startupVolumeRange?.value || 80);
    setStartupConfirmReady(false);
    setPower(0);
    syncButtonState();
    setStatus("カードをドラッグして挿入してください");
    updateHud();
    drawScene();
    requestAnimationFrame(loop);
  }

  init();
})();
